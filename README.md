First create .env file

<!-- Setup Backend API -->
.env file to add
VITE_API_URL = http://localhost:8080/jo

<!-- RUn commands -->
npm install
It will load all dependencies and update package.json(exists), if not create package.json

npm run dev (or) npm start
It will run the code locally and gives the URL

